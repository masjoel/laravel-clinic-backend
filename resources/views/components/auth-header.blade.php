 <div class="login-brand">
    {{-- <a title="Logo" href="{{ route('home') }}" class="site__brand__logo">
     @if (klien('logo') == null)
         Desaku
     @else
     <img src="{{ preg_match('/profile/i', klien('logo')) ? Storage::url(klien('logo')) : asset(klien('logo')) }}" style="height:80px; width:auto" alt="{{ klien('nama_client') }}" class="">
     @endif
    </a> --}}
 </div>